import {FORGOT_PASSWORD_STEPS, LAYOUT_MODES, SIGN_UP_STEPS, LOGIN_STEPS} from "../configs/Settings";

const state = {
	isShowInThisRelease: false,
	layoutModes: LAYOUT_MODES,
	forgotPasswordSteps: FORGOT_PASSWORD_STEPS,
	loginSteps: LOGIN_STEPS,
	signUpSteps: SIGN_UP_STEPS,
};

export default state;
